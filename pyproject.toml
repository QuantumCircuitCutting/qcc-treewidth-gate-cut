[project]
name = "qcc-treewidth-gate-cut"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []  # TODO

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
[tool.setuptools.packages.find]
include = ["treewidth_gate_cut*"]
