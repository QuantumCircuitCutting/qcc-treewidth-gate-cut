[project]
name = "qcc-treewidth-gate-cut"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = []  # TODO

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends._legacy:_Backend"
